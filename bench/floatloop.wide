# bench: a hot float while-loop — 10M iterations of f64 add/compare (F64-ABI JIT, v0.47).
fn fsum(n) {
    total = 0.0
    i = 0.0
    while i < n {
        total = total + i
        i = i + 1.0
    }
    return total
}
print(fsum(10000000.0))    # 49999995000000
