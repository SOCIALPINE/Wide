# bench: tensor matmul 128x128, 20 times — bulk-op path (T6: fast Rust ops, NumPy model).
import "std/ai"
a = ones([128, 128])
b = ones([128, 128])
i = 0
acc = 0.0
while i < 20 {
    c = matmul(a, b)
    acc = acc + c.sum().item()
    i = i + 1
}
print(acc)    # 20 * 128^3 = 41943040
