# bench: a hot integer while-loop — 10M iterations of add/compare.
fn sum_to(n) {
    total = 0
    i = 0
    while i < n {
        total = total + i
        i = i + 1
    }
    return total
}
print(sum_to(10000000))    # 49999995000000
